# YoXi邮箱 - 打包经验总结

## 一、安卓本地打包

### 1.1 环境要求

| 工具 | 版本/路径 | 说明 |
|------|-----------|------|
| JDK | 17.0.12+7 | `C:\Users\o1415\jdk-17.0.12+7` |
| Android SDK | 35.0.0 | `%LOCALAPPDATA%\Android\Sdk` |
| Flutter | 3.24.0 stable | `C:\Users\o1415\flutter` |
| Python | 3.11 | 需安装 flet==0.22.0 |
| Python Android Dist | - | `C:\Users\o1415\python-android-dist\python-android-dist` |

### 1.2 环境变量设置

```powershell
# JDK
$env:JAVA_HOME = "C:\Users\o1415\jdk-17.0.12+7"

# Android SDK
$env:ANDROID_HOME = "$env:LOCALAPPDATA\Android\Sdk"
$env:ANDROID_SDK_ROOT = "$env:LOCALAPPDATA\Android\Sdk"

# Python Android 运行时（serious_python）
$env:SERIOUS_PYTHON_BUILD_DIST = "C:\Users\o1415\python-android-dist\python-android-dist"

# PATH（必须排除 Android Studio 的 JBR，否则会冲突）
$newPath = @()
foreach ($p in $env:Path -split ';') {
    if ($p -and $p -notlike "*Android Studio*jbr*" -and $p -notlike "*jbr*") {
        $newPath += $p
    }
}
$newPath = @("$env:JAVA_HOME\bin", "$env:ANDROID_HOME\platform-tools", "C:\Users\o1415\flutter\bin") + $newPath
$env:Path = $newPath -join ';'

# Flutter 国内镜像（可选，加速依赖下载）
$env:PUB_HOSTED_URL = "https://pub.flutter-io.cn"
$env:FLUTTER_STORAGE_BASE_URL = "https://storage.flutter-io.cn"
```

### 1.3 打包命令

```powershell
# 基本打包
flet build apk --template "C:\Users\o1415\flet-build-template"

# 带参数（注意：--product/--project/--org 在 flet 0.22.0 中可能不生效）
flet build apk --template "C:\Users\o1415\flet-build-template" --product "YoXi邮箱" --project "yoxi_email" --org "com.yoxi"
```

### 1.4 输出位置

- APK 文件：`<项目目录>/build/apk/app-release.apk`
- 大小：约 45-47 MB

---

## 二、iOS 云打包（GitHub Actions）

### 2.1 工作流文件

路径：`.github/workflows/build.yml`

### 2.2 关键配置

```yaml
name: Build iOS
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install flet==0.22.0
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'
      - run: |
          sudo gem install cocoapods
          pod setup
      # 使用本地模板（含自定义图标和应用名称）
      - run: |
          flet build --verbose ipa --product "YoXi邮箱" --project "yoxi_email" --org "com.yoxi" --template "./flet-template"
```

### 2.3 重要：IPA 生成问题

**问题**：Flet build ipa 默认使用 `--no-codesign`，**不会生成 IPA 文件**，只会生成 xcarchive。

**解决方案**：在工作流中添加步骤，从 xcarchive 手动创建 IPA：

```yaml
- name: Create IPA from xcarchive
  if: always()
  run: |
    # 查找 xcarchive
    XCARCHIVE=$(find . -name "*.xcarchive" -type d 2>/dev/null | head -1)
    
    mkdir -p output
    
    # 从 xcarchive 找到 .app 文件
    APP_PATH=$(find "$XCARCHIVE" -name "*.app" -type d 2>/dev/null | head -1)
    
    if [ -n "$APP_PATH" ]; then
      # 创建 Payload 目录
      mkdir -p Payload
      cp -R "$APP_PATH" Payload/
      
      # 压缩为 IPA
      zip -r "output/YoXi邮箱.ipa" Payload/
    fi
```

### 2.4 Artifacts 上传

```yaml
- name: Upload IPA
  if: always()
  uses: actions/upload-artifact@v4
  with:
    name: YoXi-Email-iOS-IPA
    path: |
      output/*.ipa
      **/*.ipa
    retention-days: 30
```

### 2.5 下载位置

1. 打开 GitHub Actions 页面
2. 点击对应的工作流运行
3. 滚动到页面底部的 **Artifacts** 区域
4. 下载 `YoXi-Email-iOS-IPA`

---

## 三、自定义模板（关键！）

### 3.1 为什么需要自定义模板

Flet 0.22.0 的 `--product`、`--project`、`--org` 参数**可能不生效**，导致：
- 应用名称显示为目录名（如 `ios_python_app`）
- 包名默认为 `com.flet.<项目名>`
- 图标使用默认图标

**解决方案**：直接修改模板文件，把 cookiecutter 变量替换为硬编码值。

### 3.2 模板位置

- 本地模板：`C:\Users\o1415\flet-build-template`
- 仓库内模板：`./flet-template/`（用于 iOS 云打包）

### 3.3 需要修改的文件

#### Android - 应用名称和包名

**文件**：`{{cookiecutter.out_dir}}/android/app/src/main/AndroidManifest.xml`

```xml
<application
    android:label="YoXi邮箱"  <!-- 替换 {{ cookiecutter.product_name }} -->
    android:name="${applicationName}"
    android:icon="@mipmap/ic_launcher">
```

**文件**：`{{cookiecutter.out_dir}}/android/app/build.gradle`

```gradle
android {
    namespace "com.yoxi.yoxi_email"  // 替换 {{ cookiecutter.org_name }}.{{ cookiecutter.project_name }}
    ...
    defaultConfig {
        applicationId "com.yoxi.yoxi_email"  // 替换
        ...
    }
}
```

#### Android - MainActivity 包名和路径

**文件**：`{{cookiecutter.out_dir}}/android/app/src/main/kotlin/com/yoxi/yoxi_email/MainActivity.kt`

```kotlin
package com.yoxi.yoxi_email  // 替换 {{ cookiecutter.org_name }}.{{ cookiecutter.project_name }}

import io.flutter.embedding.android.FlutterActivity

class MainActivity: FlutterActivity() {
}
```

**注意**：目录路径也要从 `{{ cookiecutter.kotlin_dir }}` 改为 `com/yoxi/yoxi_email`

#### iOS - 应用名称

**文件**：`{{cookiecutter.out_dir}}/ios/Runner/Info.plist`

查找 `CFBundleName` 和 `CFBundleDisplayName`，替换为 `YoXi邮箱`

### 3.4 自定义图标（所有平台）

使用 Python PIL 库，把 3D 邮箱图标调整到不同尺寸，替换模板中所有图标文件。

```python
from PIL import Image
import os

template_dir = "path/to/flet-build-template/{{cookiecutter.out_dir}}"
app_icon_path = "path/to/app_icon.png"

original_icon = Image.open(app_icon_path).convert('RGBA')

def resize_and_save(target_path, size):
    resized = original_icon.resize((size, size), Image.LANCZOS)
    resized.save(target_path, 'PNG')

# Android mipmap 图标
android_sizes = {
    'mipmap-mdpi': 48,
    'mipmap-hdpi': 72,
    'mipmap-xhdpi': 96,
    'mipmap-xxhdpi': 144,
    'mipmap-xxxhdpi': 192,
}

# iOS AppIcon 图标（15种尺寸）
ios_sizes = {
    'Icon-App-20x20@1x.png': 20,
    'Icon-App-20x20@2x.png': 40,
    # ... 更多尺寸
    'Icon-App-1024x1024@1x.png': 1024,
}

# 其他：web/icons, macos, images/icon.png 等
```

---

## 四、常见问题和解决方案

### 4.1 安卓打包 - build 目录被占用

**错误**：`PermissionError: [WinError 32] 另一个程序正在使用此文件`

**原因**：文件资源管理器或其他程序正在访问 build 目录。

**解决方案**：
1. 关闭所有访问 build 目录的程序
2. 使用全新的项目目录打包（如 `yoxi_build2`、`yoxi_build3`）
3. 每次打包前删除旧的 build 目录

### 4.2 安卓打包 - JDK 冲突

**错误**：Gradle 编译失败，与 Android Studio 的 JBR 冲突。

**解决方案**：在 PATH 中排除 Android Studio 的 JBR：
```powershell
$newPath = @()
foreach ($p in $env:Path -split ';') {
    if ($p -and $p -notlike "*jbr*") {
        $newPath += $p
    }
}
```

### 4.3 安卓打包 - 应用打不开

**原因**：MainActivity 的包名与 AndroidManifest.xml 中的包名不一致。

**解决方案**：确保以下三处包名一致：
1. `AndroidManifest.xml` 中的 `package` 属性
2. `build.gradle` 中的 `namespace` 和 `applicationId`
3. `MainActivity.kt` 中的 `package` 声明和文件路径

### 4.4 iOS 打包 - 没有 IPA 文件

**原因**：Flet 默认使用 `--no-codesign`，不生成 IPA，只生成 xcarchive。

**解决方案**：从 xcarchive 手动创建 IPA（见 2.3 节）。

### 4.5 iOS 打包 - 应用名称/图标不正确

**原因**：`--product` 参数不生效，使用默认模板。

**解决方案**：使用自定义模板，直接修改模板文件中的应用名称和图标（见第三节）。

### 4.6 GitHub 推送失败

**错误**：`Failed to connect to github.com:443`

**原因**：网络问题，GitHub 无法访问。

**解决方案**：
1. 等待网络恢复后重试
2. 使用加速器/VPN
3. 多次重试推送

---

## 五、打包前检查清单

### 安卓打包前

- [ ] JAVA_HOME 指向 JDK 17
- [ ] PATH 中排除了 Android Studio 的 JBR
- [ ] SERIOUS_PYTHON_BUILD_DIST 已设置
- [ ] 项目目录中没有 data.json（避免自动登录）
- [ ] app_icon.png 存在于项目根目录
- [ ] 模板中的应用名称、包名、图标已修改
- [ ] 使用全新的项目目录（避免 build 目录冲突）

### iOS 云打包前

- [ ] flet-template 目录已提交到仓库
- [ ] 工作流中使用 --template "./flet-template"
- [ ] 工作流包含从 xcarchive 创建 IPA 的步骤
- [ ] 工作流包含 Artifacts 上传步骤
- [ ] 代码已推送到 GitHub main 分支

---

## 六、文件结构

```
项目根目录/
├── app_code.py              # 主程序代码
├── main.py                  # 入口文件
├── app_icon.png             # 应用图标（1024x1024）
├── config.json              # 应用配置
├── requirements.txt         # Python 依赖
├── flet-template/           # 自定义模板（用于iOS云打包）
│   └── {{cookiecutter.out_dir}}/
│       ├── android/         # Android 模板（已修改应用名称、包名、图标）
│       ├── ios/             # iOS 模板（已修改应用名称、图标）
│       └── ...
├── .github/
│   └── workflows/
│       └── build.yml        # iOS 云打包工作流
└── 打包经验.md               # 本文档
```

---

## 七、版本信息

- Flet：0.22.0
- Flutter：3.24.0 stable
- JDK：17.0.12+7
- Android compileSdk：35
- Python：3.11
- 文档更新时间：2026-08-27
