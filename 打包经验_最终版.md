# YoXi邮箱 打包经验总结

## 安卓打包（本地Windows）

### 前置条件
- Flutter SDK: 3.24.0
- Android SDK: 34.0.0
- Python: 3.11
- Flet: 0.22.0

### 打包步骤
1. **准备源代码**
   - 确保 `app_code.py`、`main.py` 是最新版本
   - 确保 `assets/` 目录包含所有资源文件

2. **创建app.zip（关键！）**
   - 使用Python的zipfile模块创建，确保路径使用正斜杠（/）
   - 包含：main.py, app_code.py, config.json, settings.json, data.json, requirements.txt, assets/
   - 验证app.zip中的app_code.py和源代码一致（MD5校验）

3. **配置gradle.properties**
   ```properties
   org.gradle.jvmargs=-Xmx2048M -XX:MaxMetaspaceSize=512m
   android.useAndroidX=true
   android.enableJetifier=true
   org.gradle.offline=true
   android.dexOptions.javaMaxHeapSize=1g
   org.gradle.parallel=false
   org.gradle.caching=true
   ```

4. **编译命令**
   ```bash
   cd flutter_build
   flutter build apk --release --no-shrink
   ```

### 关键注意事项
- **不要清理build缓存**！清理后会导致重新下载Python发行版（GitHub访问不稳定）
- **使用--no-shrink参数**！禁用代码压缩，避免内存不足（OutOfMemoryError）
- **禁用并行构建**！`org.gradle.parallel=false`，减少内存使用
- **离线模式**！`org.gradle.offline=true`，避免重新下载依赖
- **app.zip必须用Python创建**！确保路径使用正斜杠，不要用Windows的压缩工具

### 常见问题
1. **downloadDistArchive失败** - GitHub连接超时，不要清理build缓存，使用已有缓存
2. **packageRelease OutOfMemoryError** - 增加Gradle内存，使用--no-shrink，禁用并行构建
3. **白屏** - app.zip中的app_code.py不是最新版本，重新创建app.zip
4. **一直加载** - 检查app_code.py中的加载页逻辑，确保5秒后可以进入

### 输出位置
- APK路径: `flutter_build/build/app/outputs/flutter-apk/app-release.apk`
- 大小: 约42 MB

---

## iOS打包（GitHub Actions云打包）

### 前置条件
- GitHub仓库: https://github.com/Xiao-Yang-520/YoXi-
- Workflow文件: `.github/workflows/build.yml`

### 打包步骤
1. **提交最新代码**
   ```bash
   git add app_code.py main.py assets/ .github/workflows/build.yml flet-template/
   git commit -m "更新最新源代码"
   git push origin main
   ```

2. **触发GitHub Actions**
   - 推送代码后自动触发
   - 或手动触发: https://github.com/Xiao-Yang-520/YoXi-/actions

3. **等待打包完成**
   - 预计时间: 20-40分钟
   - 下载产物: Actions页面 → 最新的build → Artifacts → YoXi-Email-iOS-IPA

### Workflow配置
- 运行环境: macos-latest
- Python: 3.11
- Flet: 0.22.0
- Flutter: 3.24.0
- 构建命令: `flet build ipa --product "YoXi邮箱" --project "yoxi_email" --org "com.yoxi" --template "./flet-template"`

### 常见问题
1. **GitHub连接失败** - 使用代理或VPN，或稍后重试
2. **打包时间过长** - 正常现象，macOS环境需要安装很多依赖
3. **找不到IPA文件** - Workflow会自动从xcarchive创建IPA，检查Artifacts

---

## 应用图标配置

### 默认图标
- 路径: `assets/cute_email_icon.png`
- 名称: 可爱邮箱
- 在设置中默认选择第一个

### 图标切换
- 用户可以在设置中切换应用图标
- 图标列表: 默认图标、远程图标、QQ邮箱、网易邮箱、谷歌邮箱、移动邮箱、未知邮箱、自定义图标

### 启动页图标
- 启动页显示应用图标、应用名称、版本号
- 5秒倒计时后可以点击跳过进入
